using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CharacterInstanciateManagera : MonoBehaviour
{

    //GameObject deckHolder = DeckLoader.Instance.deckHolder;
    //List<Card> selectedDeck = DeckLoader.Instance.selectedDeck;


    ////Randon spawning 
    //public Vector3 minSpawnPos;
    //public Vector3 maxSpawnpos;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    for (int i = 0; i < selectedDeck.Count; i++)
    //    {
    //        GameObject deckSlot = deckHolder.transform.GetChild(i).gameObject;
    //        Button cardButton = deckSlot.GetComponent<Button>();
    //        cardButton.onClick.AddListener(getCharacterInstanciate);
    //    }
    //}

    

    //    public void getCharacterInstanciate()
    //{

    //    for (int i = 0; i < selectedDeck.Count; i++)
    //    {

    //        GameObject charModel = selectedDeck[i].characterModel;
    //        Vector3 RandomPos = new Vector3(Random.Range(minSpawnPos.x, maxSpawnpos.x),0, Random.Range(minSpawnPos.z, maxSpawnpos.z));
    //        Instantiate(charModel, RandomPos, charModel.transform.rotation);


    //    }
    //}
}
